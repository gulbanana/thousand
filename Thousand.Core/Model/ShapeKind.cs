namespace Thousand.Model
{
    public enum ShapeKind
    {
        RoundRectangle,
        Rectangle,
        RoundSquare,
        Square,
        Ellipse,
        Circle,
        Diamond

        Rect = Rectangle,
        RoundRect = RoundRectangle,
        Oval = Ellipse
    }
}
